# Postgres self-hosted (postgres-totum na docker network do Coolify)
# Formato: postgres://USUARIO:SENHA@HOST:5432/cashflow
DATABASE_URL=

# Segredo dos JWTs de sessão — gere com: openssl rand -hex 32
JWT_SECRET=

NODE_ENV=development
